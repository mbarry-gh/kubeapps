{
  "name": "{{gitops.name}}",
  "namespace": "{{gitops.namespace}}",
  "imageName": "k3d-registry.localhost:5000/{{gitops.imageName}}",
  "imageTag": "{{gitops.imageTag}}",
  "port": "{{gitops.port}}",
  "nodePort": "{{gitops.nodePort}}",
  "readyProbe": "{{gitops.readyProbe}}",
  "livenessProbe": "{{gitops.livenessProbe}}",
  "targets": [ "local" ]
}
